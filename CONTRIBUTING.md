# Contributing Guide

본 문서는 우리 팀의 Git 커밋 컨벤션 규칙을 설명합니다.
PR 및 코드 리뷰 전 반드시 확인해주세요.

---

## 1. 커밋 유형 지정
커밋 유형은 영어 소문자로 작성합니다.

| 커밋 유형 | 의미 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 formatting, 세미콜론 누락, 코드 자체의 변경이 없는 경우 |
| `refactor` | 코드 리팩토링 |
| `chore` | 패키지 매니저, 빌드 설정, 기타 유지보수 (예: .gitignore) |
| `test` | 테스트 코드, 리팩토링 테스트 코드 추가 |
| `design` | CSS 등 사용자 UI 디자인 변경 |
| `comment` | 필요한 주석 추가 및 변경 |
| `rename` | 파일 또는 폴더 명만 수정/이동 |
| `remove` | 파일 삭제 |
| `!BREAKING CHANGE` | 커다란 API 변경 |
| `!HOTFIX` | 긴급 치명적 버그 수정 |
| `WIP` | 작업 중(Work In Progress) → 완료 시 정리가 필요함 |

---

## 2. 제목 작성 규칙
- **형식**: `타입: [이슈번호] 제목`
- **이슈 번호**: Jira 또는 이슈 트래커의 ID를 대괄호 `[]`로 감싸서 명시 (예: `[SCRUM-83]`)
- **언어**: 한글 사용을 원칙으로 함
- **내용**: 변경 사항을 50자 이내로 간결하게 요약

---

## 3. 본문 작성 규칙
- **분리**: 제목과 본문은 **한 줄 띄워서** 분리
- **요약문**: 본문 첫 줄에 전체 변경 사항의 목적과 핵심 내용을 서술형으로 작성
- **상세 목록**: 단순 나열보다는 **`키워드: 설명`** 형식을 권장
    - **키워드**: 변경의 핵심 가치, 목적, 적용 기술 등을 명시 (예: `SSOT 확립`, `성능 개선`)
    - **설명**: 해당 변경이 왜 필요한지, 어떤 효과가 있는지 구체적으로 서술
- **구조화**: 필요시 하위 불릿(들여쓰기 2칸)을 사용하여 상세 내역을 그룹화

---

## 4. 커밋 메시지 예시

**권장 예시:**
```text
feat: [SCRUM-83] 중앙 집중식 데이터 필드 매핑 레이어(mapping.py) 구축

데이터 소스(OCS/DB)와 심사 엔진 간의 결합도를 낮추기 위해 필드명을 중앙 집중식으로 관리하는 매핑 상수를 정의

- SSOT(Single Source of Truth) 확립: 엔진 전반에서 사용하는 하드코딩된 문자열을 ClinicalFields 클래스로 일원화하여 관리 편의성 증대.
- 유연한 데이터 대응: 향후 데이터 소스의 스키마(Key)가 변경되더라도, 로직 수정 없이 mapping.py의 값(Value)만 수정하여 대응 가능한 구조 확보.
- 적용 범위:
  - 환자 기본 정보 및 각 심사 Pillar(1, 2, 3, 4, 7)별 핵심 필드 매핑.
  - 리스트 내 중첩 객체(Past History)를 위한 내부 키 상수화.
  ```